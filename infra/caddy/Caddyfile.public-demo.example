{
	email {$ACME_EMAIL:}
}

{$PUBLIC_WEB_HOST} {
	encode zstd gzip
	reverse_proxy web:3000
}

{$PUBLIC_API_HOST} {
	encode zstd gzip

	@websocket {
		header Connection *Upgrade*
		header Upgrade websocket
	}

	reverse_proxy api:8000
}
