"""Model Gateway module boundary.

Public entrypoints: api, schemas, service, permissions, events, exceptions.
Other modules must call model operations through services, not Model Gateway ORM models.
"""
