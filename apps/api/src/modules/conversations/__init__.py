"""Conversations module boundary.

Public entrypoints: api, schemas, service, permissions, events, exceptions.
Other modules must call the service layer or domain events, not Conversations ORM models.
"""
